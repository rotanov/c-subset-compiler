LL(k)
LR(k)

LL(1):

s <- sentinel, eof

таблица переходов зависит от вершины стека и текущего символа входного потока
SLR
LALR
CLR

typedef впилить обратно
struct впилить обратно:C

ДЗ закончить синтаксический анализ

Семантический анализ.
Выводдить таблицу символов.

проверку выражений во время разбора попожже
сначала весь разбот